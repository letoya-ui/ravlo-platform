# Copy to .env.local and fill in values
# Never commit .env.local to git

# Ravlo backend URL
EXPO_PUBLIC_API_URL=https://ravlo.app

# EAS Build (set in CI or via `eas secret:create`)
EXPO_TOKEN=your_expo_access_token
EAS_PROJECT_ID_ACADEMY=your_eas_project_id
